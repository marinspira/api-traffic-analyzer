{
  "name": "api-traffic-analyzer",
  "version": "1.0.4",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "api-traffic-analyzer": "ts-node src/cli.ts logs/users.log",
    "build": "tsc",
    "start": "ts-node src/cli.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "api-traffic-analyzer": "./dist/cli.js"
  },
  "keywords": ["log", "analyzer", "api", "cli", "express", "ip"],
  "author": "Maria Ferreira",
  "license": "MIT",
  "description": "Discover how many people are accessing your APIs, which endpoints they use, and from where.",
  "dependencies": {
    "commander": "^14.0.0"
  },
  "devDependencies": {
    "@types/commander": "^2.12.0",
    "@types/express": "^5.0.3",
    "@types/node": "^24.0.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
